<nav>
<h2><%= link_to "@#{@organization.name}", @organization %></h2>
</nav>

<article>

<header>
<h1>Tags</h1>
</header>

<section>
<% @tags.each do |tag| %>
  <article>
    <h2><%= link_to "##{tag.name}", [@organization, tag] %></h2>
  </article>
<% end %>
</section>

<nav>
<p>
<%= link_to 'New Tag', new_organization_tag_path(@organization) %>
</p>
</nav>

</article>

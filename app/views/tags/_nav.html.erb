<nav>
<div class="content">
<h2><%= link_to "@#{@organization.name}", @organization %></h2>
<% if @tag.present? %>
<h3><%= link_to 'Tags', organization_tags_path(@organization) %></h3>
<% end %>
</div>
</nav>
